pipeline {
    agent any

    environment {
        MAVEN_HOME = MAVEN_HOME // Ensure you have a Maven tool configured in Jenkins
        PATH = "D:\apache-maven-3.9.6-bin\apache-maven-3.9.6"
    }

    stages {
        stage('Checkout') {
            steps {
                git 'https://your-repository-url.git'
            }
        }
        
        stage('Build') {
            steps {
                script {
                    // Clean and compile the project
                    sh 'mvn clean compile'
                }
            }
        }
        
        stage('Test') {
            steps {
                script {
                    // Run the tests
                    sh 'mvn test'
                }
            }
        }
        
        stage('Package') {
            steps {
                script {
                    // Package the application
                    sh 'mvn package'
                }
            }
        }
        
        stage('Deploy') {
            steps {
                script {
                    // Deploy the application
                    // This step is optional and can be configured as per your deployment strategy
                    sh 'mvn deploy'
                }
            }
        }
    }

    post {
        always {
            // Archive the artifacts, e.g., JAR/WAR files
            archiveArtifacts artifacts: 'target/*.jar', allowEmptyArchive: true

            // Publish test results
            junit 'target/surefire-reports/*.xml'

            // Clean up workspace
            cleanWs()
        }
        success {
            echo 'Build was successful!'
        }
        failure {
            echo 'Build failed!'
        }
    }
}
